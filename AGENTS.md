# DecisionTrace 协作协议

本文件约束所有在 DecisionTrace 项目中工作的 coding agents 和人类协作者。项目目标是构建一个可审查、低误报、默认只读的产品契约漂移检测系统，而不是快速堆叠 AI 功能。

## 1. 先判断当前请求

- **解释、研究、审查、诊断、计划：** 默认只读，只报告结果。
- **明确要求修改或实现：** 直接在指定范围内编辑并完成相称验证，不重复确认安全的本地步骤。
- **外部写入：** push、发布、部署、创建/关闭 Issue 或 PR、联系用户、改权限、购买、调用付费 API、上传私有代码或数据，必须由用户在当前请求中明确授权。
- **破坏性动作：** 删除、移动、覆盖既有文件或清理其他人的分支/worktree，必须先解析精确目标；授权不清晰时停止。

讨论、方案和 AI 建议不等于用户确认。没有明确写入授权时，不顺手修改文件。

## 2. Source of truth

发生冲突时按以下顺序处理：

1. 用户在当前请求中的明确决定与授权。
2. 可回溯的真实 repo、测试结果、eval artifact、review 反馈和运行证据。
3. 本项目 canonical 文档：
   - `docs/01-PRD.md`：产品需求与范围；
   - `docs/02-ARCHITECTURE.md`：系统边界与模块；
   - `docs/03-EVALUATION.md`：评测与 release gates；
   - `docs/04-OPEN-QUESTIONS.md`：尚未决定的问题。
4. README、Issue、讨论稿和 agent 生成草稿。

AI 输出、文件存在、测试数量和措辞流畅度都不是事实真源。来源冲突时不得静默选择，必须报告冲突及依据。

## 3. 文件所有权

| 文件 | 唯一回答的问题 |
|---|---|
| `README.md` | 项目是什么、当前在哪里、从哪里进入 |
| `docs/01-PRD.md` | 为什么做、为谁做、P0 做什么、怎样验收 |
| `docs/02-ARCHITECTURE.md` | 系统怎样接入、模块如何分工、信任边界是什么 |
| `docs/03-EVALUATION.md` | 怎样建立 ground truth、评测系统并决定是否发布 |
| `docs/04-OPEN-QUESTIONS.md` | 哪些关键决定仍未知、需要什么证据 |
| `AGENTS.md` | 协作者如何安全、诚实地完成工作 |

不要创建 `final-v2`、平行 PRD、重复架构说明或新的总览真源。能更新 canonical owner 就不新建同主题文件。

## 4. 产品不可违反的原则

1. **旁路接入。** P0 不进入目标产品的用户请求关键路径。
2. **默认只读。** 发现问题不等于获得修改目标仓库的权限。
3. **Evidence or abstain。** 没有直接来源引用的候选发现不能进入正式报告。
4. **确定性优先。** 文件存在、引用、ID、路径覆盖等可由规则判断的事实，不交给 LLM 猜测。
5. **AI 只产生候选语义判断。** LLM 不得成为唯一 grader 或最终 truth owner。
6. **人类负责 disposition。** 真漂移、有意变更、误报、接受风险和证据不足必须可由 reviewer 明确区分。
7. **Hard Gate 必须保守。** 未经 shadow-mode 证明低误报的语义规则不能阻断 release。
8. **隐私最小化。** 不默认上传源代码、Prompt、用户数据、密钥、私有路径或生产 telemetry。
9. **不制造结果。** 不伪造用户、采用、节省时间、准确率、案例、外部反馈或开源贡献。

## 5. 术语与稳定 ID

- 产品契约：`CTR-###`
- 功能需求：`FR-###`
- Drift 类型：`D1` / `D2` / `D3`
- Eval case：`EV-###`
- Finding：`FND-YYYYMMDD-###`
- 技术决定：未来需要时使用 `ADR-###`

统一使用以下事实标签：

- `confirmed`：用户确认或直接证据证明；
- `observed`：运行、测试或 review 中实际观察；
- `inferred`：AI/分析者基于证据的推断；
- `proposed`：尚未批准的方案或门槛；
- `unknown`：当前证据不足。

不得把 `inferred` 或 `proposed` 写成 `confirmed`。

## 6. 每次实现前

1. 读取本文件及与改动直接相关的 canonical 文档。
2. 检查 Git status、当前分支、worktree 和用户未提交内容；不得覆盖。
3. 明确本轮问题、目标用户、范围、非范围和验收条件。
4. 先检查已有实现、测试、Issue 和决定，避免重复功能。
5. 若需要模型/API，先定义无密钥降级、成本边界和测试替身。

## 7. 实现与验证规则

- 任何 `FR-*` 必须能关联至少一个验收或 eval case。
- 任何产品行为声明必须明确由规则、代码、Test、Eval、Telemetry 或用户研究中的哪一种证据支持。
- 新增 drift detector 时必须同时增加：正例、反例、边界例和至少一个已知误报例。
- Model grader 必须在人类标注样本上校准；不能用同一模型同时生成 case、评分并宣布通过。
- 报告必须包含原始来源、事实/推断边界、置信度、严重性和推荐的人类下一步。
- 无模型、网络失败、历史不足、文件不可解析时必须降级或 abstain，不能生成看似确定的答案。
- 测试失败时先区分原有失败与本轮回归，不得删除测试来制造通过。

## 8. 数据与安全

- P0 fixtures 使用合成或明确授权并去标识化的数据。
- 不提交 `.env`、tokens、credentials、真实用户内容、私有 repo 文本或绝对个人路径。
- GitHub Action 默认最小权限：读取内容；只有明确启用 PR comment 时才申请相应写权限。
- 日志不得保存完整源代码、Prompt 或秘密；诊断输出应可配置脱敏。
- 云端模型接入必须提供 local-only / no-upload 选项或清楚声明其缺失。

## 9. 开源与对外表述

- 当前许可证未决定。用户确认许可证并完成 first-party/third-party provenance audit 前，不添加 `LICENSE`，也不对外称为 open source。
- Public repository 不自动等于 open source。
- README、Demo 和 Release Notes 中的每项结果必须能回溯到实际测试或公开 artifact。
- Coding agent 的贡献必须如实披露；AI 生成代码本身不证明用户拥有相应能力。
- 不使用目标公司的品牌、内部材料或招聘措辞暗示合作或背书。

## 10. 完成后的报告

只报告：

- 实质改动；
- 实际运行的验证及结果；
- 原有与新增失败；
- 尚未解决的风险和 Unknown；
- 等待用户决定的外部动作。

不得用文件数、代码行数、测试数量或 agent 使用时长冒充产品进展。

修改本文件后提醒用户：新的 Codex 任务或会话才保证重新加载更新后的协议。

